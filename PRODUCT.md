# Product

## Register

product

## Users

Stable Diffusion Forge Neo 用户，在本地创作工作流中使用 LLM 生成、转换、批处理和缓存 Prompt。用户通常需要快速试做一条 Prompt，或把一组已有输入转换成可直接生图的结果。

## Product Purpose

LLM Prompt Studio 把模型连接、Prompt 生成、批量转换和缓存联动放在一个可理解的工作台中。成功标准是用户能在三步内完成生成、批量处理或写回 Prompt，并能看到明确的运行状态和结果。

## Brand Personality

清晰、克制、可靠。界面服务于创作流程，避免把高级选项和内部实现暴露在主路径上。

## Anti-references

避免多层嵌套面板、同义按钮并列、需要猜测的图标按钮、无状态反馈的自动运行，以及把设置、队列和结果混在同一层级的仪表盘布局。

## Design Principles

- 生成、批处理、缓存是一级工作流，设置和工具是二级入口。
- 每个工作流只保留一个明确的主动作，停止、保存和导出按状态显示。
- 默认只显示完成当前任务所需的字段，高级参数采用渐进展开。
- 状态、禁用原因和结果位置必须在操作附近可见。
- 保留 README 中的能力，但合并重复路径，不牺牲可恢复性和批处理数据。

## Accessibility & Inclusion

遵循 WCAG 2.1 AA 的对比度和键盘可达性要求。所有按钮提供文字名称和焦点样式，运行中、禁用、错误和空结果状态可通过文本识别；尊重 reduced-motion 设置。
