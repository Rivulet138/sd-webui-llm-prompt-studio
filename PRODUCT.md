# 产品与界面约定

## Register

product

## 用户与目标

面向在 Forge Neo 中创作的用户：快速生成 Prompt，复用已有缓存，批量反推图片，并明确决定何时生图。常用任务应在一个工作区内完成，执行状态和结果位置保持可见。

## 页面职责

| 页面 | 职责 |
| --- | --- |
| 生成 | 单条／批量生成、生成去向、默认收起的缓存 Prompt 处理 |
| 缓存 | 原始记录管理、导入导出、PNG / Ranbooru 批次和交接 |
| 处理结果库 | 已处理 Prompt 的筛选、选择、写入与生图入队 |
| 设置 | 主连接、独立备用连接、共享采样与推理参数 |
| 更多 | WD14 / CL 反推和静态词库 |
| txt2img 内嵌面板 | 从 LLM 或两个缓存库获取 Prompt，写入或入队 |

## 交互约定

- 单条与批量生成共用入口，不增加同义标签页或第二套推理参数。
- 主动作随生成去向显示对应名称；停止、队列取消和结果写入使用明确的文字。
- 「仅缓存」「写入 Prompt」「入队生图」有明确边界；打开页面、切换模板、刷新或读取缓存均不自动生图。
- 处理原始缓存时默认另存结果；覆盖操作单独展开，范围必须明确。
- 「所选结果」为空时提示选择，不自动扩大为全部记录。
- 标明 `0 = 无限` 的生成数量允许持续运行，并始终提供停止入口；有限集合的读取上限另行标注。
- 进度显示已完成、未完成与失败数量，失败项保留可重试入口。
- 工作室与 txt2img 的快速模板、已保存推理设置保持一致。

## 视觉与可访问性

主表单使用清晰字号和随内容增长的文本框，正面 Prompt 保留足够编辑空间。高级参数、缓存处理和模板编辑默认收起，避免固定大空框和深层折叠。

列表保留必要字段，不重复展示正面摘要、标签摘要和隐藏元数据。长表格在容器内滚动，窄屏控件换行，页面本身避免横向溢出。

按钮提供文字名称和可见焦点；状态不能只靠颜色表达。对比度与键盘操作以 WCAG 2.1 AA 为目标，尊重减少动画偏好。新增控件需检查桌面与窄屏布局，并验证元素 ID 唯一、事件绑定有效。
