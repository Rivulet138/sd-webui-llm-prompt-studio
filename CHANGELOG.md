# Changelog

## 2026-09-22

- 修复文生图内嵌面板误走服务端队列、参数回落到默认值的问题，改为逐轮点击 Forge 原生生成按钮，使用当前文生图参数及脚本设置。
- 内嵌入口明确为前端连续生图，数量 `0` 表示无限，正数按轮数完成；显示完成轮数，保留停止及 LoRA 编辑保护。
- 合并重复生图按钮；前端循环启动立即返回，避免长期占用 Gradio 按钮回调。

## 2026-09-21

- 修复连续生图启动期间恢复旧 Prompt 时，覆盖用户新增或删除 LoRA 的问题；保留提交等待、取消和超时期间的手动修改。
- 删除重复项目介绍和独立界面约定文档，维护约定合入开发指南；保留 README、更新记录和开发指南，更新文档导航。

## 2026-09-20

### 生成与队列

- 合并单条与批量生成，可选择仅保存到缓存或自动加入生图队列。
- 一级面板和 txt2img 内嵌面板的生成数量支持 `0 = 无限`，提供停止、失败反馈及结果保留；无限生图逐条等待完成，避免持续堆积队列。
- 主、备用 LLM 可使用不同服务商、地址、模型和凭据；内嵌生成复用已保存的推理参数。
- 快速模板在一级面板与 txt2img 同步，支持编辑、默认选择和删除；System Prompt 保留一个生效编辑框。
- 生图队列读取当前 txt2img 参数并显示进度和图片，不再要求填写 API 生图参数。
- 修复启动或刷新时触发生图的问题；队列执行须由用户明确提交。

### 缓存与界面

- 缓存 Prompt 处理移至生成页并默认收起，支持转换、扩写、润色、完成数量统计和继续未完成任务。
- 新增独立的 `processed_prompt_cache.db` 存储处理结果，支持所选／全部筛选结果入队，防止空选误提交与同批重复入队。
- txt2img 在同一个面板选择 LLM 自动生成、原始缓存库或处理结果库。
- 修复内嵌入口仅轮换前 1000 条缓存的问题，两个缓存源均按记录 ID 逐条读取，支持大库、记录删除和新增，不增加浏览器内存占用。
- PNG Collector / Ranbooru 主动读取与 JSON 批次导入集中到缓存页，保留结果写回和导出。
- 缓存采用分页、筛选和批量编辑，导出统一为全部原始缓存。
- 删除重复的批量生成、直接导入和 img2img 面板，移除相似 Prompt 检索与重复摘要；放大正面编辑区并改进窄屏布局。
- 静态词库保留目录分类，选中词条可追加到固定 Prompt，索引失败保留旧数据。

### 图片反推

- 内置 WD14 / CL Tagger 1.01 ONNX 反推，自动检测本地模型，支持额外模型目录。
- 提供显式模型下载及手动下载链接，不自动下载、不预选推荐模型。
- 支持单图和文件夹／子文件夹批量反推、停止、继续未完成和重试失败。
- 结果可保存到缓存或送入 txt2img；批量结果接入 Forge 的 `Prompts from File or Textbox`。
- 修复 Forge 恢复模块搜索路径后回调无法导入 WD14 模块的问题。

### 文档与验证

- 重写使用说明、项目介绍和界面约定，补充开发验证指南。
- 增加缓存处理、模板同步、模型适配、零数量持续生成、大库读取、队列边界及浏览器交互回归覆盖。

## 2026-09-06

- 在首屏显示当前模型模板适配说明，明确生成、转换、扩写、润色按底模同步。
- 提升生成按钮、输出区、状态块和键盘焦点的可视层级。
- 增加减少动画偏好和窄屏布局支持。
- 补充 About、模型模板边界和官方提示词工程参考。
- 增加 Ranbooru 缓存到 LLM JSON 批处理的直达入口，支持按筛选结果执行转换、扩写和润色。
